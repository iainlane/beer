extends layout

block content
    p welcome to the secret area muhahah
    form(method='post', enctype='multipart/form-data')
      input(type='file', name='imageupload')
      input(type='submit', name='uploadimage', value='Upload Image')
    if img
        img(src=`/images/${img}`)
